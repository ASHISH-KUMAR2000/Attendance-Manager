<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ashish.attendancemanager">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".TeacherClassAttendance"
            android:label="@string/title_activity_teacher_class_attendance"
            android:theme="@style/AppTheme.NoActionBar"></activity>
        <activity android:name=".TeacherAddClass" />
        <activity android:name=".TeacherActivity" />
        <activity android:name=".AddRemoveStudentActivity" />
        <activity android:name=".AdminStudentsActivity" />
        <activity android:name=".AddRemoveTeacherActivity" />
        <activity android:name=".AdminTeachersActivity" />
        <activity android:name=".AddRemoveCourseActivity" />
        <activity android:name=".AdminCoursesActivity" />
        <activity android:name=".AdminActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>